export interface NounType {
  word: string;
  article: string;
  translation: string;
}

export interface AdjectiveType {
  word: string;
  translation: string;
}

export interface VerbType {
  word: string;
  translation: string;
  example: string;
}
